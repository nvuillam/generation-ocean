{
    "name": "generation-ocean",
    "version": "0.1.0",
    "scripts": {
      "postinstall": "npm install --prefix backend && npm install --only=dev --no-shrinkwrap --prefix backend  && npm run build --prefix backend"
    }
  }